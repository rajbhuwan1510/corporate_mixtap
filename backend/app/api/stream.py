# Stream endpoints have been removed.
# Audio playback is handled entirely client-side via the YouTube IFrame Player API.
# The backend only provides search results (videoId + metadata) and room sync state.
